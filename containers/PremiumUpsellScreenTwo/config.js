'use strict';
const ScreenConfig = () => {
  // TODO: add keys in for the other categories, like basic info, etc, etc
  return {
    'config' : [
      {
        'image': '',
        'headline': '',
        'text' : '',
      }
    ]
  }
}

export default ScreenConfig
